<div class="container">
    <div class="row home">
      <div class="col-xs-12">
        <% @article.each do |a|%>
          <h2><%= link_to a.title, article_path(a) %></h2>
          <p><%= a.content %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>
